# Legacy note: the old text-based compact-KPP parameter file is no longer used by
# the active evaluator path.
#
# Current ShogiAI expects a binary ShogiAI-KPP-v1 model at:
#   - src/eval/kpp.bin
#   - build/eval/kpp.bin (copied by CMake)
#
# This file is kept only as a migration note so older references do not silently
# activate the removed distance-based heuristic.
